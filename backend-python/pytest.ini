[pytest]
markers =
    unit: fast isolated unit tests
    integration: API and pipeline integration tests
    live: hits external services (EIA, etc.); skip in CI with -m "not live"
testpaths = tests
pythonpath = .
